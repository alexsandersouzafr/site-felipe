pre-commit:
  commands:
    biome:
      glob: "*.{js,jsx,ts,tsx,json,jsonc,css,md}"
      run: pnpm biome check --write {staged_files}
      stage_fixed: true

pre-push:
  commands:
    typecheck:
      run: pnpm exec tsc --noEmit
    tests:
      run: pnpm test
