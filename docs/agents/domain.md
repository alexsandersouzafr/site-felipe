# Domain docs

This is a single-context repository.

Before exploring domain code, read `CONTEXT.md` and any relevant decisions in `docs/adr/`. Use the glossary vocabulary defined in `CONTEXT.md`; flag conflicts with an existing ADR rather than silently overriding it.
